linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - ineffassign
    - typecheck
    - unused
    - misspell
    - gosec
    - prealloc
    - unconvert
    - unparam

run:
  deadline: 5m
  tests: true
  skip-dirs:
    - test/
    - vendor/

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
        - errcheck

output:
  format: colored-line-number
  print-issued-lines: true
  print-linter-name: true 